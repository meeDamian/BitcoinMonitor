<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pl.d30.bitcoin"
    android:versionCode="22"
    android:versionName="1.5.4" >

    <uses-sdk
        android:minSdkVersion="17"
        android:targetSdkVersion="18" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >



        <!-- DashClock extension BTC -->
        <service android:name=".dash.service.BitoinDashService"
            android:icon="@drawable/icon_small"
            android:label="@string/bitcoin_label"
            android:permission="com.google.android.apps.dashclock.permission.READ_EXTENSION_DATA">

            <intent-filter>
                <action android:name="com.google.android.apps.dashclock.Extension" />
            </intent-filter>

            <meta-data
                android:name="protocolVersion"
                android:value="2" />

            <meta-data
                android:name="description"
                android:value="@string/dashclock_desc" />

            <meta-data
                android:name="worldReadable"
                android:value="true" />

            <meta-data
                android:name="settingsActivity"
                android:value=".dash.conf.BitcoinDashConf" />

        </service>
        <activity android:name=".dash.conf.BitcoinDashConf"
            android:label="@string/bitcoin_label"
            android:exported="true" />



        <!-- DashClock extension LTC -->
        <service android:name=".dash.service.LitecoinDashService"
            android:icon="@drawable/icon_small_ltc"
            android:label="@string/litecoin_label"
            android:permission="com.google.android.apps.dashclock.permission.READ_EXTENSION_DATA">

            <intent-filter>
                <action android:name="com.google.android.apps.dashclock.Extension" />
            </intent-filter>

            <meta-data
                android:name="protocolVersion"
                android:value="2" />

            <meta-data
                android:name="description"
                android:value="@string/dashclock_desc" />

            <meta-data
                android:name="worldReadable"
                android:value="true" />

            <meta-data
                android:name="settingsActivity"
                android:value=".dash.conf.LitecoinDashConf" />

        </service>
        <activity android:name=".dash.conf.LitecoinDashConf"
            android:label="@string/litecoin_label"
            android:exported="true" />



    </application>

</manifest>